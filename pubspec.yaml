name: tshock_server_rest_client
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  get:
  tshock_server_rest:
    git:
      url: git@github.com:KohlsAdrian/tshock_server_rest.git
      ref: master

flutter:
  uses-material-design: true